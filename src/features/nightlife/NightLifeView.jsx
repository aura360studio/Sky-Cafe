import { SectionTitle } from '../../shared/components/SectionTitle';

export const NightLifeView = () => {
  return (
    <div className="view-container">
      <SectionTitle title="Sky @ Night Life" subtitle="Book tables, parties and more." />
      <div style={{ padding: '20px', textAlign: 'center', color: 'var(--text-secondary)' }}>
        [Night Life UI Layout logic will weave here] 🥂
      </div>
    </div>
  );
};
